import 'package:flutter/material.dart';

class IconsColor {
  static const Color menuColor = Color(0xffffffff);
  static const Color searchColor = Color(0xffffffff);
}

const menuColor = IconsColor.menuColor;

const searchColor = IconsColor.searchColor;
